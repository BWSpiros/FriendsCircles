<h1>Users</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Email</th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.id %></td>
      <td><%= link_to user.email, user_url(user) %><td>
    </tr>
  <% end %>
</table>
<p>
  <%= link_to "Make a new User!", new_user_url %>
</p>